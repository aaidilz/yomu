import { initializeApp } from "firebase/app";
import { getFirestore } from "firebase/firestore";

const firebaseConfig = {
  apiKey: "{value}",
  authDomain: "{value}",
  projectId: "{value}",
  storageBucket: "{value}",
  messagingSenderId: "{value}3",
  appId: "{value}",
  measurementId: "{value}",
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
export const db = getFirestore(app);
